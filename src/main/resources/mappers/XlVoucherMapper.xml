<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
"http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.finance.dao.XlVoucherDao">
    <resultMap type="XlVoucher" id="XlVoucherResult">
		<result property="id" column="id"/>
		<result property="name" column="name"/>
		<result property="price" column="price"/>
		<result property="flag" column="flag"/>
		<result property="type" column="type"/>
		<result property="validate" column="validate"/>
		<result property="vo_num" column="vo_num"/>
		<result property="goodId" column="goodId"/>
		<result property="stipulate_price" column="stipulate_price"/>
		<result property="create_user" column="create_user"/>
		<result property="update_user" column="update_user"/>
	</resultMap>
	
    <select id="getXlVouchers" resultType="XlVoucher">
 		select * from xl_voucher
 	</select>
 	
    <select id="findXlVoucher" parameterType="Map" resultMap="XlVoucherResult">
		select * from xl_voucher
		<where>
		    <if test="name!=null and name!='' ">
				name like #{name}
			</if>
		</where>
		<if test="start!=null and size!=null">
			limit #{start},#{size}
		</if>
	</select>
	
    <select id="getTotalXlVoucher" parameterType="Map" resultType="Long">
		select count(*) from xl_voucher
		<where>
		    <if test="name!=null and name!='' ">
				name like #{name}
			</if>
		</where>
	</select>
	
    <insert id="addXlVoucher" parameterType="XlVoucher">
		insert into xl_voucher(id,name,price,flag,type,validate,vo_num,goodId,stipulate_price,create_user) values(null,#{name},#{price},#{flag},#{type},#{validate},#{vo_num},#{goodId},#{stipulate_price},#{create_user});
	</insert>
	
    <update id="updateXlVoucher" parameterType="XlVoucher" >
		update xl_voucher
		<set>
			<if test="name!=null and name!='' ">
				name=#{name}
			</if>
			<if test="price!=null and price!='' ">
				price=#{price},
			</if>
			<if test="flag!=null and flag!='' ">
				flag=#{flag},
			</if>
			<if test="type!=null and type!='' ">
				type=#{type},
			</if>
			<if test="validate!=null and validate!='' ">
				validate=#{validate},
			</if>
			<if test="vo_num!=null and vo_num!='' ">
				vo_num=#{vo_num},
			</if>
			<if test="goodId!=null and goodId!='' ">
				goodId=#{goodId},
			</if>
			<if test="stipulate_price!=null and stipulate_price!='' ">
				stipulate_price=#{stipulate_price},
			</if>
			<if test="create_user!=null and create_user!='' ">
				a.age=#{age},
			</if>
			<if test="update_user!=null and update_user!='' ">
				update_user=#{update_user},
			</if>
			a.updatetime=#{updatetime}
		</set>
		where id=#{id}
	</update>
	
    <delete id="deleteXlVoucher" parameterType="Integer">
	    delete from xl_voucher where id=#{id};
	</delete>

</mapper> 	